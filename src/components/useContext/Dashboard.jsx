import { Profile, Sidebar } from './Components';

const Dashboard = ({}) => {
     
    return (
        <div>
            <Sidebar />
            <Profile />
        </div>
    );
};

export default Dashboard;
